{
  "name": "agent-payments-reference",
  "version": "0.0.1",
  "private": true,
  "description": "An AI agent with its own identity, wallet and bounded spending authority.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
